package com.creditwise.app.data.model;

public enum Direction {
    /** Money in (credit of the account). Shown with a leading "+" in the statement. */
    CREDIT,
    /** Money out (debit of the account). */
    DEBIT
}
